# Market Mobility Insights v3 — Phase 1

Included:
- original dashboard UI
- latest three month tabs generated automatically
- current month and previous two months stored separately
- NewsAPI discovery across configured country/category searches
- Netlify Blobs shared storage
- review and approval workflow
- duplicate removal by URL
- Coverage Monitor
- Review Queue page
- live FX endpoint
- verified monthly fuel-data endpoint
- daily scheduled discovery

Pages:
- `/` main dashboard
- `/coverage.html` coverage monitor
- `/review.html` review queue

APIs:
- `/api/news`
- `/api/review-queue`
- `/api/review-action`
- `/api/refresh-news`
- `/api/coverage-live`
- `/api/fx`
- `/api/fuel`
